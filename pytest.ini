[pytest]
addopts = --strict-markers
markers =
    stress: long-running stress and robustness tests
    extreme: high-cost pre-freeze and nightly tests
    property: hypothesis-based invariant/property tests
    soak: long-running repeatability and high-iteration tests
