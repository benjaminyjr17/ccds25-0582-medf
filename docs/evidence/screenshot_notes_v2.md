# Screenshot Notes v2

The Demo Scenario button was clicked (AI System changed to "Hiring Screening (Demo)") but the KPI cards still show N/A. The demo scenario populates the Case Studies page but doesn't auto-run the Evaluate page. Need to trigger evaluation explicitly on each page by clicking the action buttons. Will create a more comprehensive script that clicks "Evaluate", "Detect Conflicts", and "Generate Pareto" buttons on each respective page.
